scan:
  num_stocks: 500
  lookback_days: 30
  morning_minutes: 20

signals:
  # Sweet spot from Monte Carlo sweep
  stock_jump_min: 2.00
  dji_jump_min_bps: 3.0
  premarket_minutes: 10

alpaca:
  feed: "iex"
  max_requests_per_min: 200

output:
  results_csv: "output/buy_signals.csv"
  signals_csv: "output/signals_detail.csv"

cache:
  db_path: "cache/market_data.db"

dji_proxy:
  symbol: "DIA"
