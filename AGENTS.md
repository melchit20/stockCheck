## Cursor Cloud specific instructions

This is a minimal repository containing only a `test.txt` file. There are no applications, services, build steps, or dependencies to install.

- **Lint/Test/Build:** No lint, test, or build commands are configured.
- **Run:** No runnable application exists. The repository content is a single `test.txt` file.
- **Dependencies:** None. No package manager or dependency files are present.
